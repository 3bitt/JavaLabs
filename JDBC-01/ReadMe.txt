
Program is working in console / Terminal
Program is connecting to Student.db and allow to perform some actions on it. (CRUD)

When run first time:
If program can't find Student.db in user directory it creates new database called Student.db.
  - next time user runs program it automatically connects to earlier created DB.
